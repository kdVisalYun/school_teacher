import BackButton from "components/__reusable/BackButton";
import Main from "./Main";

const UploadPicturePage = () => {
  return (
    <section className="h-full space-y-3 lg:space-y-5">
      <BackButton />
      <Main />
    </section>
  );
};

export default UploadPicturePage;
